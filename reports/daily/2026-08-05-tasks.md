# Unified Task Dashboard - 2026-08-05

> **ERROR: Task sync could not complete**

## Integration Authentication Required

This scheduled run was unable to gather task data because the following MCP integrations require authorization and cannot complete OAuth in a non-interactive (scheduled) session:

| Integration | Status | Required For |
|-------------|--------|--------------|
| Notion MCP | **Needs auth** | Sprint/backlog pages, Daily Task Dashboard page |
| Atlassian/Jira MCP | **Needs auth** | Active Jira issues (status != Done, updated last 7 days) |

## Action Required

To fix this for future runs:

1. Open an **interactive** Claude Code session (web or desktop)
2. Go to **Connector Settings** on claude.ai and authorize both:
   - **Notion** connector
   - **Atlassian** connector
3. Re-run this scheduled task — it will complete successfully once both connectors are authenticated

## What This Task Would Produce

When authenticated, this daily task produces:
- A unified task table across Notion + Jira with columns: Source, Title, Priority, Status, Due Date, Platform
- Platform mapping to: AssetGrid, BizFlow, OrionGrid, NeoVibe, OpsFlow, Q-GRID, or Other
- Tasks ranked: P0 overdue > P0 due today > P1 overdue > P1 due today > P2 > P3
- This markdown report committed to `reports/daily/`
- A Notion page titled "Daily Task Dashboard" updated with the same content

**Summary:** 0 total tasks gathered — connector authentication required before data can be fetched.
