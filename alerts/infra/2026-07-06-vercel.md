## Vercel Health Alert - 2026-07-06

| Project | Status | Last Deployment | Issues |
|---------|--------|-----------------|--------|
| mater-maria | ❌ ERROR | dpl_2aahKgY29KthP2bpR2BjU15Xba6M | No Next.js version detected — Root Directory misconfigured |
| gridera | ❌ ERROR | dpl_7z6KyBiC1yzZKVCCD3dzxbHtwCNo | Prisma WASM module missing — `@prisma/client` generate fails (persistent since Apr 2026) |
| web | ❌ ERROR | dpl_8hFsTWbdwedxUuDsG6vZzg9U9dMK | No Next.js version detected — Root Directory misconfigured (3+ consecutive failures) |
| orca-api-backend | ❌ ERROR + 🚫 BLOCKED | dpl_7vAsG9qLjTKJRaxeDuEYuHthtJby | `pyproject.toml` missing `[project]` table — `uv lock` fails; latest deploy BLOCKED |
| social-media-orchestra | 🚫 BLOCKED | — | All 6 deployments BLOCKED — never reached production |
| sma-api-prod | 🚫 BLOCKED | dpl_67syuYscT1srcuSoVy2DpiNcQ2NE | Latest deployment BLOCKED |
| landing | ✅ READY | — | Previously had ERROR; recovered via Node 24 + lockfile fix (PR #21) |
| comply | ✅ READY | — | Previously had ERROR; recovered via Node 24 + lockfile fix (PR #21) |
| leads-2026-06-16 | ⬜ NO DEPLOYS | — | No deployments found |
| bizflow-backend | ⬜ NO DEPLOYS | — | No deployments found |
| neovibe-studio | ⬜ NO DEPLOYS | — | No deployments found |
| (remaining 39 projects) | ✅ READY | — | Healthy |

---

## Failed Deployment Details

### mater-maria — `dpl_2aahKgY29KthP2bpR2BjU15Xba6M` (ERROR)
Branch: `feat/prompt-bible-integration` | Commit: `feat: wire Ad Campaign Prompt Bible into campaign pipeline`

```
06:47:58  Vercel CLI 54.14.0
06:48:05  Error: No Next.js version detected. Make sure your package.json has "next" in
          either "dependencies" or "devDependencies". Also check your Root Directory
          setting matches the directory of your package.json file.
```

**Recommended Fix:** In Vercel project settings for `mater-maria`, update the **Root Directory** to point to the subdirectory containing the Next.js app's `package.json` (e.g. `apps/mater-maria`). A recent commit restructured the repo into a monorepo but the Vercel root was not updated to match.

---

### gridera — `dpl_7z6KyBiC1yzZKVCCD3dzxbHtwCNo` (ERROR — persistent since Apr 2026)
Branch: `main` | Commit: `chore: remove internal file from public repo`

```
02:24:51  npm error path /vercel/path0/products/gridera/web
02:24:51  npm error command sh -c prisma generate
02:24:51  npm error Cannot find module
          '/vercel/path0/node_modules/@prisma/client/runtime/query_engine_bg.postgresql.wasm-base64.js'
02:24:51  npm error Require stack:
02:24:51  npm error - /vercel/path0/node_modules/prisma/build/index.js
02:24:51  Error: Command "npm install" exited with 1
```

**Recommended Fix:** The `prisma generate` postinstall script runs at monorepo root but Vercel resolves the WASM path relative to the product subdirectory. Options:
1. Override the Vercel **Build Command** for this project to `cd products/gridera/web && npx prisma generate && npm run build`
2. Pin `@prisma/client` to a non-WASM output target (remove `output = "edge"` from `generator client` in `schema.prisma`)
3. Or move `schema.prisma` into the product-level directory and generate locally before build

---

### web — `dpl_8hFsTWbdwedxUuDsG6vZzg9U9dMK` (ERROR — 3+ consecutive failures)

```
13:29:28  Vercel CLI 53.3.2
13:29:34  Error: No Next.js version detected. Make sure your package.json has "next" in
          either "dependencies" or "devDependencies". Also check your Root Directory
          setting matches the directory of your package.json file.
```

**Recommended Fix:** Same root cause as `mater-maria` — verify the **Root Directory** in Vercel project settings points to the folder containing the Next.js `package.json`. Check whether a recent refactor moved the app into a monorepo subdirectory without updating the Vercel project config.

---

### orca-api-backend — `dpl_7vAsG9qLjTKJRaxeDuEYuHthtJby` (ERROR) + latest BLOCKED

```
11:43:57  WARNING! Due to `builds` existing in your configuration file, the Build and
          Development Settings defined in your Project Settings will not apply.
11:43:57  Error: Failed to run "uv lock --python /vercel/path1/.vercel/python/.venv/bin/python"
11:43:57  error: No `project` table found in: /vercel/path1/pyproject.toml
```

**Recommended Fix:** Add a `[project]` table to `pyproject.toml`. The `uv lock` command requires a valid PEP 517 project definition:

```toml
[project]
name = "orca-api-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []
```

Also investigate why the latest deployment is BLOCKED — likely a Vercel concurrent build limit or plan cap.

---

### social-media-orchestra + sma-api-prod (BLOCKED)
No build logs available — deployments are BLOCKED before the build starts. `social-media-orchestra` has **never** reached production across all 6 recorded deployments.

**Recommended Fix:** Check Vercel team plan for concurrent build limits. Trigger a manual redeploy after verifying plan capacity. If the project requires a paid plan feature (e.g. Edge Middleware, higher compute), confirm billing is active.

---

*Report generated by automated health monitor on 2026-07-06*
