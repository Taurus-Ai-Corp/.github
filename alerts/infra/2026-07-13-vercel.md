## Vercel Health Alert - 2026-07-13

| Project | Status | Last Deploy | Branch | Issues |
|---------|--------|-------------|--------|--------|
| landing | ✅ READY | 2026-07-13 | main (GRIDERA) | None |
| comply | ✅ READY | 2026-07-13 | main (GRIDERA) | None |
| orca-ai | ✅ READY | 2026-07-13 | main | None |
| social-media-orchestra | ✅ READY | 2026-07-13 | main | None |
| nexus-creative-editorial | ✅ READY | 2026-07-13 | main | None |
| nexus-creative-live | ✅ READY | 2026-07-13 | main | None |
| nexus-platform | ✅ READY | 2026-07-13 | feat/unify-nexus-creative-campaigns | Preview only |
| **mater-maria** | ❌ ERROR | 2026-07-13 | feat/unify-nexus-creative-campaigns | npm peer dep conflict |
| **mater-maria** | ❌ ERROR | 2026-07-13 | add-claude-github-actions-* | Missing routes-manifest.json |
| **mater-maria** | ❌ ERROR | 2026-07-13 | add-claude-github-actions-* | Missing routes-manifest.json |
| 42 other projects | — | No deploys in last 24h | — | Inactive / not deployed |

---

**Failed Deployment Details:**

### 1. `dpl_2MrY6EC1GV3SPu9cCnymnfcDFZb4` — branch `feat/unify-nexus-creative-campaigns` (10:05 UTC)

Commit: `fix(platform): restore design-system.css lost in Wave 2a repo reorg`

```
npm error ERESOLVE could not resolve

While resolving: @eslint/js@10.0.1
Found: eslint@9.39.4
  dev eslint@"^9.39.4" from the root project

Could not resolve dependency:
  peerOptional eslint@"^10.0.0" from @eslint/js@10.0.1
  Conflicting peer dependency: eslint@10.7.0

Fix the upstream dependency conflict, or retry this command with --force
or --legacy-peer-deps to accept an incorrect dependency resolution.

Error: Command "npm install" exited with 1
```

**Root cause:** `package.json` specifies `@eslint/js@^10.0.1` (requires `eslint@^10`) but pins `eslint@^9.39.4`. These are incompatible.

---

### 2 & 3. `dpl_GVFFux6KS4kbdn7caaXBkHsvQykv` + `dpl_Eqgptbi1jfQx1DhnFVr9LcGbJi1V` — branch `add-claude-github-actions-1783931978829` (08:40 UTC)

Commits: `"Claude PR Assistant workflow"` and `"Claude Code Review workflow"`

```
Error: The file "/vercel/path0/web/dist/routes-manifest.json" couldn't be found.
This is usually caused by:
1. The "Output Directory" setting is misconfigured (expected .next, got web/dist).
2. If using Turborepo, ensure ".next/**" is in turbo.json outputs.
3. The build command did not complete successfully.
```

**Root cause:** Vercel's Output Directory is set to `web/dist` but the Next.js build outputs to `.next`. The build does complete (Vite/Rollup finishes and produces `web/dist`), but Vercel's Next.js adapter then can't find `routes-manifest.json` in the expected `.next` location — indicating a framework mismatch: the project builds with Vite but is registered in Vercel as a Next.js project.

---

**Recommended Fixes:**

**Fix #1 — mater-maria ESLint peer conflict (`feat/unify-nexus-creative-campaigns`):**
- Option A (recommended): Downgrade `@eslint/js` to `^9.x` to match the locked `eslint@^9.39.4`:
  ```json
  "@eslint/js": "^9.0.0"
  ```
- Option B: Upgrade `eslint` to `^10` and align all `@typescript-eslint` packages to v10-compatible versions.
- Either way, delete `package-lock.json` and re-lock after the change.

**Fix #2 — mater-maria Output Directory mismatch (`add-claude-github-actions-*`):**
- In Vercel project settings → Build & Output Settings, change **Output Directory** from `web/dist` to `.next`.
- OR: If this app intentionally uses Vite (not Next.js), change the Vercel Framework Preset from "Next.js" to "Vite" so Vercel does not expect `routes-manifest.json`.
- The Claude GitHub Actions commits themselves are not the cause — they only added workflow YAML files, not build config changes. The output-directory misconfiguration likely pre-exists on this branch.
