# Vercel Deployment Health Alert — 2026-06-02

**Generated:** 2026-06-02T00:00:00Z
**Team:** `team_ljtVg59YsYUDbIdetyyOVg05`
**Fleet size:** 50 projects
**Scan window:** Last 24 hours (no new deployments detected across entire fleet)

---

## Executive Summary

| Severity | Count | Projects |
|----------|-------|---------|
| 🔴 CRITICAL | 3 | `bio-foundry-web` (BLOCKED), `gridera` (ERROR ×10), `web` (ERROR ×7) |
| 🟡 WARNING | 2 | `bizflow-backend`, `neovibe-studio` (never deployed) |
| 🟡 INFO | 4 | `neosync-dashboard`, `nexus-social-suite`, `bizflow-marketing-site`, `yqg-assistant` (READY, recent ERRORs in history) |
| 🟢 HEALTHY | 41 | All other projects — latest deployment READY |

---

## Critical Issues

### 1. `bio-foundry-web` — BLOCKED

| Field | Value |
|-------|-------|
| **Status** | `BLOCKED` |
| **Consecutive failures** | 3 |
| **Latest deployment** | `dpl_EnGzhvn4X8KhVaYJ4ZH4zexCiwP7` |
| **Triggering commit** | `refactor: migrate Hedera Hashgraph references to Hiero` |
| **Branch** | `main` |
| **Actor** | gemini |
| **Last READY deployment** | `dpl_7MLu2VPkHq4XN8EunaBqGwz8dHB3` (commit: `fix: correct all factual claims`) |

**Build logs:** Empty — deployment was blocked before the build phase started.

**Root cause:** `BLOCKED` state indicates the deployment was prevented from starting. Likely causes:
- Team spend limit reached
- Deployment protection policy blocking non-human actors (actor: gemini)
- Vercel account/billing hold

**Recommended fix:**
1. Check Vercel team billing/spend limits: Settings → Billing
2. Check deployment protection rules in the `bio-foundry-web` project settings
3. If spend limit: upgrade plan or increase the budget cap
4. Re-trigger deployment after resolving the block

---

### 2. `gridera` — ERROR (≥10 consecutive)

| Field | Value |
|-------|-------|
| **Status** | `ERROR` |
| **Consecutive failures** | ≥10 |
| **Latest deployment** | `dpl_7z6KyBiC1yzZKVCCD3dzxbHtwCNo` |
| **Triggering commit** | `chore: remove internal file from public repo` (sha: `23a0ecc`) |
| **Branch** | `main` |
| **Source repo** | `Taurus-Ai-Corp/gridera-comply` |
| **Last READY deployment** | `dpl_9N4FS3mH7V7HZCUYja8wHuiJEP88` |

**Build log excerpt:**
```
Running build in Washington, D.C., USA (East) – iad1
Cloning github.com/Taurus-Ai-Corp/gridera-comply (Branch: main, Commit: 23a0ecc)
Running "install" command: `cd server && npm install`...
added 520 packages, audited 521 packages in 11s
21 vulnerabilities (8 low, 9 moderate, 4 high)
> comply-q-grid-client@0.0.0 build
> tsc -b && vite build
vite v7.3.1 building client environment for production...
transforming...
✓ 1857 modules transformed.
[ERROR — build failed after transform, logs truncated at 50-line limit]
```

**Root cause:** Build reaches Vite transform (1857 modules) then fails. The commit `chore: remove internal file from public repo` likely deleted a file that is still referenced by import statements in the codebase, causing a post-transform bundling or TypeScript (`tsc -b`) error.

**Recommended fix:**
1. Review the diff for commit `23a0ecc` in `gridera-comply` — identify which file was removed
2. Search for any `import` or `require` referencing that removed path
3. Either restore the file or remove the dangling imports
4. Push a fix commit to `main` to re-trigger deployment

---

### 3. `web` — ERROR (≥7 consecutive)

| Field | Value |
|-------|-------|
| **Status** | `ERROR` |
| **Consecutive failures** | ≥7 |
| **Latest deployment** | `dpl_8hFsTWbdwedxUuDsG6vZzg9U9dMK` |
| **Triggering commit** | `fix: add API domain to CSP connect-src — was blocking all fetch requests` (sha: `1f3e7d4`) |
| **Branch** | `main` |
| **Last READY deployment** | `dpl_E2cheVJHq57zswsDXQDTho7ypqoj` |

**Build log excerpt:**
```
Vercel CLI 53.3.2
Installing dependencies...
added 168 packages in 6s
Warning: Could not identify Next.js version, ensure it is defined as a project dependency.
Error: No Next.js version detected. Make sure your package.json has "next" in either
       "dependencies" or "devDependencies". Also check your Root Directory setting
       matches the directory of your package.json file.
```

**Root cause:** Vercel cannot locate Next.js in the project. The triggering commit (`fix: add API domain to CSP connect-src`) may have reorganized the project structure or altered a monorepo layout, causing the Vercel Root Directory setting to no longer point to the folder that contains `package.json` with `next` as a dependency.

**Recommended fix:**
1. Open Vercel project settings → General → Root Directory
2. Verify the path points to the directory containing `package.json` with `"next"` listed as a dependency
3. If the repo structure changed with commit `1f3e7d4`, update Root Directory to match the new layout
4. Alternatively, confirm `next` is present in `dependencies` or `devDependencies` in `package.json` at the configured root

---

## Warning: Never-Deployed Projects

| Project | Status | Notes |
|---------|--------|-------|
| `bizflow-backend` | `NO_DEPLOYMENTS` | Project exists in Vercel but has zero deployment history |
| `neovibe-studio` | `NO_DEPLOYMENTS` | Project exists in Vercel but has zero deployment history |

These projects may be intentionally pending or stale placeholder entries. No immediate action required unless these services are expected to be live.

---

## Info: READY Projects with Recent ERROR History

| Project | Current Status | Notes |
|---------|---------------|-------|
| `neosync-dashboard` | `READY` | `dpl_3MaBykUaj5ymnZhw45KecXbJacw4` ERROR present in recent history |
| `nexus-social-suite` | `READY` | Older ERRORs under previous name `social-suite-dashboard` |
| `bizflow-marketing-site` | `READY` | Older ERRORs in history, currently stable |
| `yqg-assistant` | `READY` | Older ERRORs in history, currently stable |

---

## Healthy Projects (41)

All remaining 41 projects have their latest deployment in `READY` state with no persistent failure patterns detected.

---

## Action Items

| Priority | Action | Owner | Project |
|----------|--------|-------|---------|
| P0 | Investigate BLOCKED status — check spend limit / deployment protection policy | DevOps | `bio-foundry-web` |
| P0 | Find removed file in commit `23a0ecc`, restore it or remove dangling imports | Engineering | `gridera` |
| P0 | Fix Vercel Root Directory setting or restore `next` to `package.json` | Engineering | `web` |
| P1 | Decide whether to deploy or archive these stale projects | Engineering | `bizflow-backend`, `neovibe-studio` |
| P2 | Review recent ERROR deployment `dpl_3MaBykUaj5ymnZhw45KecXbJacw4` | Engineering | `neosync-dashboard` |
