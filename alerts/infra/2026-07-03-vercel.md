## Vercel Health Alert — 2026-07-03

**Routine:** Scheduled deployment health monitor
**Checked:** 50 projects across team `taurus-s-projects`
**Window:** Last 24 hours (no new deployments in period); latest deployment state per project evaluated
**Result:** 6 projects with ERROR or BLOCKED state

---

### Project Status Summary

| Project | Status | Latest Deployment | Branch / Trigger | Repo |
|---------|--------|-------------------|-----------------|------|
| `gridera` | ❌ ERROR | `dpl_7z6KyBiC1yzZKVCCD3dzxbHtwCNo` | main — "chore: remove internal file from public repo" | `gridera-comply` |
| `web` | ❌ ERROR | `dpl_8hFsTWbdwedxUuDsG6vZzg9U9dMK` | main — "fix: add API domain to CSP connect-src" | `neosync` |
| `mater-maria` | ❌ ERROR | `dpl_2aahKgY29KthP2bpR2BjU15Xba6M` | `feat/prompt-bible-integration` (preview) | `Nexus` |
| `social-media-orchestra` | 🚫 BLOCKED | `dpl_9SGQpWwrNyPhqnaMKFxPGhuQGBtb` | initial commit — never successfully deployed | — |
| `sma-api-prod` | 🚫 BLOCKED | `dpl_67syuYscT1srcuSoVy2DpiNcQ2NE` | initial commit — never successfully deployed | — |
| `orca-api-backend` | 🚫 BLOCKED | `dpl_7CLy3MMACBhoLxgSvFGH1LquXcPT` | only production deploy is ERROR | — |

All other **44 projects** are in ✅ READY state.

---

### Failed Deployment Details

#### 1. `gridera` — ERROR

**Inspector:** https://vercel.com/taurus-s-projects/gridera/7z6KyBiC1yzZKVCCD3dzxbHtwCNo

**Build error (last 50 lines, errors only):**
```
02:24:51  npm error code 1
02:24:51  npm error path /vercel/path0/products/gridera/web
02:24:51  npm error command failed
02:24:51  npm error command sh -c prisma generate
02:24:51  npm error Prisma schema loaded from prisma/schema.prisma
02:24:51  npm error Error:
02:24:51  npm error Cannot find module '/vercel/path0/node_modules/@prisma/client/runtime/query_engine_bg.postgresql.wasm-base64.js'
02:24:51  npm error Require stack:
02:24:51  npm error - /vercel/path0/node_modules/prisma/build/index.js
02:24:51  npm error A complete log of this run can be found in: /vercel/.npm/_logs/2026-04-14T02_24_20_625Z-debug-0.log
02:24:51  Error: Command "npm install" exited with 1
```

**Root cause:** `prisma generate` fails during `npm install` because `@prisma/client/runtime/query_engine_bg.postgresql.wasm-base64.js` is missing from the installed node_modules. This is a known Prisma + Vercel monorepo hoisting issue — the Prisma CLI cannot find its own generated runtime artifact.

**Recommended fix:**
- Add `"postinstall": "prisma generate"` to the `products/gridera/web/package.json` scripts if not already present, OR
- Explicitly set the `installCommand` in `vercel.json` to run `npm install && cd products/gridera/web && npx prisma generate`
- Alternatively, upgrade `prisma` and `@prisma/client` to the same latest version and clear the Vercel build cache

---

#### 2. `web` — ERROR

**Inspector:** https://vercel.com/taurus-s-projects/web/8hFsTWbdwedxUuDsG6vZzg9U9dMK

**Build error (last 50 lines, errors only):**
```
13:29:28  Vercel CLI 53.3.2
13:29:30  npm warn deprecated @studio-freight/lenis@1.0.42: The '@studio-freight/lenis' package has been renamed to 'lenis'. Please update your dependencies
13:29:34  Error: No Next.js version detected. Make sure your package.json has "next" in either "dependencies" or "devDependencies". Also check your Root Directory setting matches the directory of your package.json file.
```

**Root cause:** Vercel cannot locate `next` in the project's `package.json`. This is almost certainly a misconfigured **Root Directory** in the Vercel project settings — the project is pointing to the repo root of `neosync` but the Next.js app lives in a subdirectory. The trigger commit ("fix: add API domain to CSP connect-src") suggests active development; the root directory may have been inadvertently reset.

**Recommended fix:**
- In the Vercel dashboard for project `web`, go to **Settings → General → Root Directory** and ensure it points to the correct subdirectory (e.g. `apps/web` or `frontend`) within the `neosync` repo
- Re-deploy after correcting the setting

---

#### 3. `mater-maria` — ERROR (preview branch)

**Inspector:** https://vercel.com/taurus-s-projects/mater-maria/2aahKgY29KthP2bpR2BjU15Xba6M

**Branch:** `feat/prompt-bible-integration` (preview deploy; production is still READY)

**Build error (last 50 lines, errors only):**
```
06:47:58  Vercel CLI 54.14.0
06:48:05  Error: No Next.js version detected. Make sure your package.json has "next" in either "dependencies" or "devDependencies". Also check your Root Directory setting matches the directory of your package.json file.
```

**Root cause:** Same "No Next.js version detected" error as `web`. The `feat/prompt-bible-integration` branch likely introduced a change to the repo structure (e.g. moved the Next.js app directory or altered `package.json`) that breaks Vercel's framework detection.

**Recommended fix:**
- Check what changed in `feat/prompt-bible-integration` in the `Nexus` repo relative to the working production branch
- Verify the `package.json` at the configured Root Directory still contains `"next"` in dependencies
- If the app was moved, update the Root Directory setting in Vercel for preview branches or fix the branch structure

---

#### 4–6. `social-media-orchestra`, `sma-api-prod`, `orca-api-backend` — BLOCKED

These three projects have never had a successful production deployment. All deployments are BLOCKED or ERROR from their initial commits. No runtime logs are available for BLOCKED deployments.

**Recommended fix:**
- Verify these projects are intentionally provisioned (not accidental forks/stubs)
- Check Vercel team billing / project limits — BLOCKED state often indicates a plan or spend limit issue
- If these are active projects, review the initial configuration (framework, root directory, environment variables) and trigger a fresh deployment

---

*Generated by TAURUS AI deployment health monitor — 2026-07-03*
