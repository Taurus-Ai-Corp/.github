## Vercel Health Alert - 2026-09-01

| Project | Status | Last Deploy | Issues |
|---------|--------|-------------|--------|
| comply | ❌ ERROR | 2026-09-01 13:27 UTC | Dependabot PR #63 — `@noble/hashes` 1.8.0→2.2.0 breaks `@taurus/pqc-crypto` TypeScript imports |
| landing | ❌ ERROR | 2026-09-01 13:27 UTC | Same root cause as `comply` (shared monorepo, same dependabot commit `83d651f8`) |
| q-grid-comply-ca | ❌ ERROR | 2026-09-01 13:27 UTC | Same root cause as `comply` |
| mater-maria | ❌ ERROR (ongoing) | 2026-08-31 13:36 UTC | npm ERESOLVE: `@eslint/js@10` vs `eslint@9` — unresolved since 2026-08-28 |
| All other 46 projects | ✅ READY / No activity | — | None |

---

**Failed Deployment Details:**

---

### comply / landing / q-grid-comply-ca

**Trigger:** Dependabot PR #63 on `taurus-ai-corp/GRIDERA` — batch bump of 21 packages (minor + patch), including `@noble/hashes` 1.8.0 → 2.2.0
**Commit:** `83d651f8`
**Deployed:** 2026-09-01 ~13:27 UTC (simultaneous across all three projects)
**Branch:** Dependabot branch targeting `main`

**Build error (identical on comply, landing, q-grid-comply-ca):**
```
src/agility.ts(13,26): error TS2307: Cannot find module '@noble/post-quantum/ml-dsa' or its corresponding type declarations.
src/kem.ts(1,27): error TS2307: Cannot find module '@noble/post-quantum/ml-kem' or its corresponding type declarations.
src/sign.ts(1,26): error TS2307: Cannot find module '@noble/post-quantum/ml-dsa' or its corresponding type declarations.
ELIFECYCLE  Command failed with exit code 2.
Error: Command "turbo run build" exited with 2
```

**Root Cause:**
`@noble/post-quantum` (the PQC library used by `@taurus/pqc-crypto`) declares a peer dependency on `@noble/hashes ^1.x`. The `@noble/hashes` v2 release changed its subpath export structure, breaking the module resolution for `@noble/post-quantum`'s `ml-dsa` and `ml-kem` subpath imports at TypeScript compile time. All three projects share this package through the GRIDERA monorepo.

**Last successful production deployments:** ~2026-08-11 (READY on `feat/certify-comply-messaging`)

**Recommended Fix:**

Option A (preferred — pin `@noble/hashes` back to 1.x in `@taurus/pqc-crypto`):
In `packages/pqc-crypto/package.json`:
```diff
- "@noble/hashes": "^2.2.0",
+ "@noble/hashes": "^1.8.0",
```
Also add a `peerDependencies` constraint or `overrides` in the monorepo root `package.json` to prevent future auto-bumps from crossing the major boundary.

Option B (close/revert dependabot PR #63): Reject the `@noble/hashes` bump from the batch PR. Dependabot can be re-allowed to bump it separately once `@noble/post-quantum` publishes compatibility with hashes v2.

Option C (upgrade `@noble/post-quantum`): Check whether a newer release of `@noble/post-quantum` supports `@noble/hashes ^2.x`. If available, upgrade both together.

**Monorepo root `package.json` override to prevent recurrence:**
```json
"overrides": {
  "@noble/hashes": "^1.8.0"
}
```

---

### mater-maria

**Trigger:** Commit `612111be` on branch `feat/nexus-core-organic-design-system` (Nexus repo)
**Deployed:** 2026-08-31 13:36 UTC
**Status:** Persistent ERROR — 13+ consecutive failures since 2026-08-28; last READY production deployment ~2026-05-13

**Build error:**
```
npm error code ERESOLVE
npm error ERESOLVE could not resolve
npm error
npm error While resolving: @eslint/js@10.0.1
npm error Found: eslint@9.39.4
npm error node_modules/eslint
npm error   dev eslint@"^9.39.4" from the root project
npm error
npm error Could not resolve dependency:
npm error peerOptional eslint@"^10.0.0" from @eslint/js@10.0.1
npm error
npm error Conflicting peer dependency: eslint@10.9.1
npm error
npm error Fix the upstream dependency conflict, or retry this command
npm error with --force or --legacy-peer-deps
npm error
Error: Command "npm install" exited with 1
```

**Root Cause:**
`package.json` on `feat/nexus-core-organic-design-system` declares `@eslint/js@^10.0.1` (requires `eslint@^10`) alongside `eslint@^9.39.4`. Incompatible peer dependency range; `npm install` aborts before any build step runs. Issue has been ongoing since 2026-08-28 — see previous alerts for full history.

**Recommended Fix (unchanged from previous alerts):**

Option A (preferred): Bump `eslint` to `^10.0.0`. `@typescript-eslint/eslint-plugin@8.61.1` supports ESLint 10.
```diff
- "eslint": "^9.39.4",
+ "eslint": "^10.0.0",
```

Option B: Pin `@eslint/js` back to `^9.x`:
```diff
- "@eslint/js": "^10.0.1",
+ "@eslint/js": "^9.0.0",
```

Option C (workaround): Add `--legacy-peer-deps` to Vercel install command in project settings or `vercel.json`.

**Note:** This branch carries 4+ weeks of undeployed commits. Resolution is overdue.
