## Vercel Health Alert - 2026-07-30

| Project | Status | Last Deploy | Issues |
|---------|--------|-------------|--------|
| q-grid-comply-ca | ⚠️ ERRORS (preview) | 2026-07-30 | 5 dependabot PR builds failing (day 3) |
| landing | ⚠️ ERRORS (preview) | 2026-07-30 | 5 dependabot PR builds failing (day 3) |
| comply | ⚠️ ERRORS (preview) | 2026-07-30 | 5 dependabot PR builds failing (day 3) |
| operable-readiness-dist | ✅ READY | 2026-07-29 | None |
| 46 other projects | ✅ No activity | No deployments in 24h | None |

> **Production is healthy.** Production deployments on `main` (commit `635be5f9`) are READY for all three GRIDERA apps. All 15 failing deployments are on unmerged dependabot preview branches.

---

## Failed Deployment Details

All three affected projects (`q-grid-comply-ca`, `landing`, `comply`) share the **GRIDERA** monorepo. The same 5 dependabot PRs that have been failing since 2026-07-28 triggered fresh builds today and are still failing.

**15 total ERROR deployments** — 5 per project × 3 projects.

### Error 1 — `minor-and-patch` group (PR #48) + `@noble/hashes` 2.x (PR #54)

**Most recent failure:** `dpl_Hn57Lra5JiAJVQCzRMPuSqtrhtuq` (q-grid-comply-ca)  
**Branch:** `dependabot/npm_and_yarn/minor-and-patch-2028fde2b0`  
**Packages bumped (18 total):** `@taurus-ai/swarm-spawner` 0.4.0→0.5.1, `drizzle-orm` 0.39.3→0.45.2, `next` 16.2.9→16.2.12, `react` 19.2.7→19.2.8, and 14 others

```
@taurus/pqc-crypto:build: src/agility.ts(13,26): error TS2307:
  Cannot find module '@noble/post-quantum/ml-dsa' or its corresponding type declarations.
@taurus/pqc-crypto:build: src/kem.ts(1,27): error TS2307:
  Cannot find module '@noble/post-quantum/ml-kem' or its corresponding type declarations.
@taurus/pqc-crypto:build: src/sign.ts(1,26): error TS2307:
  Cannot find module '@noble/post-quantum/ml-dsa' or its corresponding type declarations.
@taurus/pqc-crypto:build: ELIFECYCLE Command failed with exit code 2.
ERROR: Command "turbo run build" exited with 2
```

Root cause: `@taurus-ai/swarm-spawner` 0.5.x or `@noble/hashes` 2.x brings an incompatible version of `@noble/post-quantum`, causing subpath exports `ml-dsa`/`ml-kem` to fail to resolve in `packages/pqc-crypto`.

### Error 2 — `@neondatabase/serverless` 0.10.4 → 1.1.0 (PR #53)

**Most recent failure:** `dpl_2WLkaaZMorVmV1NFaggUaQferXA3` (q-grid-comply-ca)  
**Branch:** `dependabot/npm_and_yarn/neondatabase/serverless-1.1.0`

```
@taurus/db:build: src/guard-keys.ts(1,41): error TS2307:
  Cannot find module 'node:crypto' or its corresponding type declarations.
@taurus/db:build: src/resolve.ts(47,45): error TS2580:
  Cannot find name 'process'. Do you need to install type definitions for node?
  Try `npm i --save-dev @types/node`.
@taurus/db:build: ELIFECYCLE Command failed with exit code 2.
ERROR: Command "turbo run build" exited with 2
```

Root cause: `@neondatabase/serverless` 1.x removed its bundled Node.js type declarations. The `@taurus/db` package now needs `@types/node` added explicitly to its `devDependencies`.

### Error 3 — `typescript` 5.9.3 → 7.0.2 (PR #52)

**Most recent failure:** `dpl_EBPGu25ZtHea67QP8QQHqyiizdk2` (q-grid-comply-ca)  
**Branch:** `dependabot/npm_and_yarn/typescript-7.0.2`

```
comply:build: > Build failed because of webpack errors
comply:build: ELIFECYCLE Command failed with exit code 1.
ERROR: Command "turbo run build" exited with 1
```

Root cause: TypeScript 7.0 introduces breaking changes to module resolution and type emission that cause webpack to fail during the Next.js build of the `comply` app.

### Error 4 — `stripe` 21.0.1 → 22.4.0 (PR #55) — logs consistent with API type mismatch

### Error 5 — `@noble/hashes` 1.8.0 → 2.2.0 (PR #54) — same `@noble/post-quantum` breakage as Error 1

---

## Recommended Fix

**Production is not affected.** These are now failing for the 3rd consecutive day. The dependabot PRs need manual attention.

1. **PR #54 (@noble/hashes 2.x) / PR #48 (minor-and-patch group):**  
   In `packages/pqc-crypto/package.json`, add a resolution/override pinning `@noble/post-quantum` to a version compatible with `@noble/hashes` 2.x, or pin `@noble/hashes` to `^1.x` in the workspace root `package.json`. Check if `@noble/post-quantum` has a 2.x-compatible release.

2. **PR #53 (@neondatabase/serverless 1.x):**  
   Add `"@types/node": "..."` to `packages/db/package.json` devDependencies. Review the [Neon 1.x changelog](https://github.com/neondatabase/serverless/blob/main/CHANGELOG.md) for any API changes in `guard-keys.ts` and `resolve.ts`.

3. **PR #52 (TypeScript 7.0.2):**  
   Audit `tsconfig.json` for `moduleResolution` settings and ensure compatibility with TypeScript 7 module resolution changes. Likely requires setting `"moduleResolution": "bundler"` and verifying `paths` config for `@/` aliases.

4. **PR #55 (stripe 22.x):**  
   Update the Stripe API version string in the Stripe constructor call to match what stripe-node v22 expects.

**Recommended action:** Close PRs #52, #53, #54, #55 and re-open once the compatibility issues are resolved in a coordinated update (not via dependabot auto-bumps). PR #48 can be fixed by resolving the `@noble/post-quantum` conflict first. If left open, dependabot will continue generating failed preview deploys daily.
