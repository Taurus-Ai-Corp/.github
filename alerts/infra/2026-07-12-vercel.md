# Vercel Deployment Health Report — 2026-07-12

**Generated:** 2026-07-12 (automated monitor)
**Team:** Taurus AI (`team_ljtVg59YsYUDbIdetyyOVg05`)
**Projects scanned:** 50
**Healthy (latest deploy READY):** 43 ✅
**Degraded / failing:** 7 ⚠️

---

## Summary

| Severity | Project | State | Last Healthy | Root Cause |
|----------|---------|-------|-------------|------------|
| 🔴 FRESH ERROR | `mater-maria` | 2 ERRORs today, production still READY (older) | Current prod OK | npm peer-dep conflict (see below) |
| 🟡 RECOVERED | `platform` | 1 ERROR today → READY on next deploy | Recovered | Same npm peer-dep; fixed in follow-up commit |
| 🔴 BROKEN | `social-media-orchestra` | BLOCKED (all 6 deploys failed) | Never healthy | Initial commit — first deploy never succeeded |
| 🔴 BROKEN | `sma-api-prod` | BLOCKED (production target) | Unknown | — |
| 🔴 BROKEN | `orca-api-backend` | BLOCKED (latest); ERROR (production) | Unknown | — |
| 🔴 STALE | `gridera` | Latest 10 deploys ERROR | ~March 2026 | Persistent build failure since Mar 2026 |
| 🔴 STALE | `web` | Latest 7 deploys ERROR | ~Feb 2026 | Persistent build failure since Feb 2026 |

---

## Fresh Errors (Last 24 Hours)

### `mater-maria` — 2 ERROR deployments

Both failures occurred on branch `feat/unify-nexus-creative-campaigns` (repo: `Nexus`) with these commits:

| Deployment ID | Timestamp | Commit |
|--------------|-----------|--------|
| `dpl_2KA8QkZUTF6TwVVzGeidUqguCdFg` | 2026-07-11 ~10:19 UTC | feat(platform): unify nexus-creative-editorial campaign pages under /campaigns/ with Vercel functions |
| `dpl_2HqZydLpVpd6FeFDLD3vMJvZRGJF` | 2026-07-11 ~10:19 UTC | fix(platform): consolidate API routes to stay within Vercel Hobby 12-function limit |

**Current production** (`dpl_HsyiZRHeZkvd8VAv8UGMw16Dhh2Z`) is READY from an earlier commit — **end users are not impacted** — but the branch is broken and cannot be merged until fixed.

#### Build Log (errors only — `dpl_2HqZydLpVpd6FeFDLD3vMJvZRGJF`)

```
10:20:26  npm error code ERESOLVE
10:20:26  npm error ERESOLVE could not resolve
10:20:26  npm error
10:20:26  npm error While resolving: @eslint/js@10.0.1
10:20:26  npm error Found: eslint@9.39.4
10:20:26  npm error node_modules/eslint
10:20:26  npm error   dev eslint@"^9.39.4" from the root project
10:20:26  npm error   ...
10:20:26  npm error
10:20:26  npm error Could not resolve dependency:
10:20:26  npm error peerOptional eslint@"^10.0.0" from @eslint/js@10.0.1
10:20:26  npm error node_modules/@eslint/js
10:20:26  npm error   dev @eslint/js@"^10.0.1" from the root project
10:20:26  npm error
10:20:26  npm error Conflicting peer dependency: eslint@10.6.0
10:20:26  npm error Fix the upstream dependency conflict, or retry this command
10:20:26  npm error with --force or --legacy-peer-deps
10:20:27  Error: Command "npm install" exited with 1
```

**Root cause:** `package.json` specifies `@eslint/js@^10.0.1` (which needs `eslint@^10`) alongside `eslint@^9.39.4`. These are incompatible. The `platform` project hit the same error and self-healed with a follow-up commit on the same branch — check what that commit changed; it likely pinned `@eslint/js` to a `^9.x` compatible version or bumped `eslint` to `^10`.

**Recommended fix:**
```bash
# Option A — upgrade eslint to v10
npm install eslint@^10 --save-dev

# Option B — downgrade @eslint/js to v9-compatible
npm install @eslint/js@^9 --save-dev
```

### `platform` — 1 ERROR, RECOVERED

Same branch and same npm ERESOLVE error as `mater-maria`. A follow-up commit on `feat/unify-nexus-creative-campaigns` (deployment `dpl_*` at ts=1783678775622) resolved it — current production is READY. No action needed.

---

## Persistent Broken Projects

These projects have been failing for an extended period (not a fresh regression today). They require dedicated investigation.

### `social-media-orchestra`
- All 6 recorded deployments are BLOCKED or ERROR
- Initial commit "feat: initial commit - Social Media Orchestra"
- **Never had a healthy production deploy**
- Likely a configuration issue from project creation (missing build command, wrong framework, or environment variables not set)

### `sma-api-prod`
- Latest production deployment: **BLOCKED**
- No healthy production deploy visible in recent history

### `orca-api-backend`
- Latest deployment: **BLOCKED** (no production target)
- Second deployment: **ERROR** (production)
- No healthy production in recent history

### `gridera`
- Latest 10 deployments: all **ERROR**
- Last known READY production: ~March 2026 (`ts≈1772685413604`)
- Has been broken for ~4 months

### `web`
- Latest 7 deployments: all **ERROR**
- Last known READY production: ~February 2026 (`ts≈1774330102377`)
- Has been broken for ~5 months

---

## Healthy Projects (43/50)

All remaining 43 projects have their latest deployment in **READY** state with an active production target. No action required.

---

## Recommended Actions

| Priority | Action |
|----------|--------|
| HIGH | Fix `mater-maria` eslint peer-dep conflict on `feat/unify-nexus-creative-campaigns` — check `platform`'s recovery commit for the exact fix |
| MEDIUM | Investigate `social-media-orchestra`, `sma-api-prod`, `orca-api-backend` — likely missing env vars or misconfigured build settings |
| LOW | Decide whether `gridera` and `web` are still active; if so, unblock their build pipelines; if abandoned, consider archiving |
