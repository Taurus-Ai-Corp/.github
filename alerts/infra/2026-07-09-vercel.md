## Vercel Health Alert - 2026-07-09

| Project | Status | Last Deployment | Issues |
|---------|--------|-----------------|--------|
| gridera | ❌ ERROR | dpl_7z6KyBiC1yzZKVCCD3dzxbHtwCNo | Prisma WASM module missing — `@prisma/client` generate fails (**persistent since Apr 2026; 85+ days unresolved**) |
| web | ❌ ERROR | dpl_8hFsTWbdwedxUuDsG6vZzg9U9dMK | No Next.js version detected — Root Directory misconfigured (**persistent since ~May 2026; 54+ days unresolved**) |
| social-media-orchestra | 🚫 BLOCKED | dpl_9SGQpWwrNyPhqnaMKFxPGhuQGBtb | All 6 deployments BLOCKED/ERROR since Jun 17 — billing/plan restriction; never reached production |
| orca-api-backend | 🚫 BLOCKED + ❌ ERROR | dpl_7CLy3MMACBhoLxgSvFGH1LquXcPT | Latest preview BLOCKED; production ERROR — uv lock fails, `[project]` table missing from pyproject.toml |
| sma-api-prod | 🚫 BLOCKED | dpl_67syuYscT1srcuSoVy2DpiNcQ2NE | Latest prod BLOCKED since Jun 17; previous READY still serving |
| mater-maria | ⚠️ Preview ERROR | dpl_HsyiZRHeZkvd8VAv8UGMw16Dhh2Z | Prod READY (Jun 16); preview branch still ERROR (Root Directory misconfigured) |
| leads-2026-06-16 | ⬜ NO DEPLOYS | — | No deployments found |
| bizflow-backend | ⬜ NO DEPLOYS | — | No deployments found |
| neovibe-studio | ⬜ NO DEPLOYS | — | No deployments found |
| (remaining 41 projects) | ✅ READY | — | Healthy — all confirmed responding |

---

## Failed Deployment Details

### gridera — `dpl_7z6KyBiC1yzZKVCCD3dzxbHtwCNo` (ERROR — persistent since Apr 2026, **~85 days**)
Branch: `main` | Commit: `chore: remove internal file from public repo` | Repo: `gridera-comply`

```
02:24:51  npm error path /vercel/path0/products/gridera/web
02:24:51  npm error command sh -c prisma generate
02:24:51  npm error Cannot find module
          '/vercel/path0/node_modules/@prisma/client/runtime/query_engine_bg.postgresql.wasm-base64.js'
02:24:51  npm error Require stack:
02:24:51  npm error - /vercel/path0/node_modules/prisma/build/index.js
02:24:51  Error: Command "npm install" exited with 1
```

**Status:** Unchanged from Jul 8 alert — unresolved for 85+ days. All 10 recent production deployments are ERROR. Last successful prod: ~Mar 4, 2026 (from `Comply.Q-Grid` repo, now stale).

**Recommended Fix (escalated — 85 days):**
1. Set Vercel **Root Directory** to `products/gridera/web`
2. Or override the **Build Command** to: `cd products/gridera/web && npx prisma generate && npm run build`
3. Or remove `output = "edge"` from `generator client` in `schema.prisma` to avoid WASM target

---

### web — `dpl_8hFsTWbdwedxUuDsG6vZzg9U9dMK` (ERROR — persistent, **~54 days**)

```
13:29:34  Error: No Next.js version detected. Make sure your package.json has "next" in
          either "dependencies" or "devDependencies". Also check your Root Directory
          setting matches the directory of your package.json file.
```

**Status:** Unchanged from Jul 8 alert — 7 consecutive production ERRORs since ~May 17, 2026. Last successful prod: `dpl_E2cheVJHq57zswsDXQDTho7ypqoj` (~Mar 24, 2026, serving stale code).

**Recommended Fix:** In Vercel project settings for `web`, update the **Root Directory** to the folder containing the Next.js `package.json` (likely `apps/web` or similar after monorepo restructure).

---

### social-media-orchestra — ALL BLOCKED / No Production (since Jun 17, 2026)
All 6 deployments (5 BLOCKED + 1 ERROR) from commit `aa80ead1` ("feat: initial commit - Social Media Orchestra"). This project has never successfully reached production.

**Build error (dpl_6ZtPAsWG3CnDMgGULaM5jD3rW6W3 — the one ERROR before blocking started):**
```
11:01:29  Error: No FastAPI entrypoint found in default locations, but found potential entrypoints:
11:01:29    backend/api/__init__.py (variable: app)
11:01:29    backend/api/main.py (variable: app)
11:01:29  Add this to your pyproject.toml:
11:01:29  [tool.vercel]
11:01:29  entrypoint = "backend.api.__init__:app"
```

**Recommended Fix:**
1. Add to `pyproject.toml` in repo root: `[tool.vercel]` / `entrypoint = "backend.api.__init__:app"`
2. Also check Vercel plan concurrent build limits — subsequent deployments were BLOCKED (likely hit after the first ERROR triggered retries)

---

### orca-api-backend — BLOCKED + ERROR / No Production (since Jun 17, 2026)
2 deployments: 1 BLOCKED (preview), 1 ERROR (production). Triggered from `social-media-orchestra` initial commit — likely a mis-configured project pointing to the wrong repo.

**Build error (dpl_7vAsG9qLjTKJRaxeDuEYuHthtJby):**
```
11:43:57  WARNING! Due to `builds` existing in your configuration file, the Build and
          Development Settings defined in your Project Settings will not apply.
11:43:57  Error: Failed to run "uv lock --python /vercel/path1/.vercel/python/.venv/bin/python"
11:43:57  error: No `project` table found in: /vercel/path1/pyproject.toml
```

**Recommended Fix:**
1. Add a `[project]` table to `pyproject.toml` (required by uv)
2. Or replace `uv lock` with `pip install -r requirements.txt` in `vercel.json` builds config
3. Verify the Vercel project source repo is correct — this appears to be pulling from `social-media-orchestra` repo

---

### sma-api-prod + social-media-orchestra — BLOCKED (Plan/Billing)
Deployments BLOCKED before build starts. Multiple projects simultaneously blocked on Jun 17, 2026 from the same commit, suggesting a concurrent build limit was hit.

**Recommended Fix:** Check Vercel team plan at vercel.com/teams/taurus-s-projects/settings/billing for concurrent build limits or seat restrictions. Trigger a manual redeploy after confirming plan capacity.

---

*Report generated by automated health monitor on 2026-07-09*
