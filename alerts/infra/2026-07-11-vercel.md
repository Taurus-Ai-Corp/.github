## Vercel Health Alert - 2026-07-11

| Project | Status | Last Deploy | Issues |
|---------|--------|-------------|--------|
| nexus-creative-live | READY | 2026-07-11 | None |
| nexus-creative-editorial | READY | 2026-07-11 | None |
| platform | READY (recovered) | 2026-07-11 | Prior deploy `dpl_9RSf8ktgu9SCkAUtrqQkNu7SftGJ` errored; subsequent deploy succeeded |
| **mater-maria** | **ERROR** | **2026-07-11** | **Both recent deployments failed — no healthy deploy in 24h window** |
| All other 46 projects | No activity | — | No deployments in last 24h |

---

**Failed Deployment Details:**

**Project:** `mater-maria`
**Branch:** `feat/unify-nexus-creative-campaigns`
**Repo:** `Taurus-Ai-Corp/Nexus`

Failed deployments:
- `dpl_2HqZydLpVpd6FeFDLD3vMJvZRGJF` — ERROR (latest, ~10:20 UTC 2026-07-10)
- `dpl_2KA8QkZUTF6TwVVzGeidUqguCdFg` — ERROR (earlier, same branch)

**Error log excerpt:**
```
npm error code ERESOLVE
npm error ERESOLVE could not resolve
npm error Could not resolve dependency:
npm error peerOptional eslint@"^10.0.0" from @eslint/js@10.0.1
npm error Conflicting peer dependency: eslint@10.6.0
npm error (project has eslint@"^9.39.4" installed)
npm error Fix the upstream dependency conflict, or retry this command
npm error with --force or --legacy-peer-deps
Error: Command "npm install" exited with 1
```

Root cause: `package.json` declares `@eslint/js@^10.0.1` (requires ESLint v10) alongside `eslint@^9.39.4` (v9). These are incompatible peer dependencies and npm refuses to resolve them.

---

**Recommended Fix:**

Choose one of the following approaches in `mater-maria`'s `package.json` on branch `feat/unify-nexus-creative-campaigns`:

1. **Upgrade ESLint to v10** (recommended if the codebase supports it):
   ```json
   "eslint": "^10.0.0"
   ```

2. **Downgrade `@eslint/js` to match ESLint v9**:
   ```json
   "@eslint/js": "^9.0.0"
   ```

3. **Quick workaround** — add to `package.json` to suppress the conflict:
   ```json
   "npmrc": "legacy-peer-deps=true"
   ```
   or add `.npmrc` with `legacy-peer-deps=true`.

The same commit (`de65c6f`) was deployed to `platform` on the same branch — the platform build succeeded, suggesting the `mater-maria` project has a different `package.json` with the conflicting `@eslint/js@^10.0.1` dependency not present in the platform project.
