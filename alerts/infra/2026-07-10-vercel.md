## Vercel Health Alert - 2026-07-10

> Scanned 50 projects across team `taurus-s-projects`. 4 projects had deployments in the last 24 hours. **1 project has unresolved failures.**

### Projects Active in Last 24h

| Project | Status | Last Deploy | Issues |
|---------|--------|-------------|--------|
| nexus-creative-live | ✅ READY | 2026-07-10 (×2) | None |
| nexus-creative-editorial | ✅ READY | 2026-07-10 | None |
| platform | ⚠️ RECOVERED | 2026-07-10 (1 ERROR → 1 READY) | Prior deploy exceeded Vercel Hobby 12-function limit (self-healed) |
| **mater-maria** | **❌ ERROR** | **2026-07-10 (×2 both ERROR)** | **npm ERESOLVE: ESLint peer dependency conflict** |

> 46 projects had no deployments in the last 24 hours and are not listed.

---

### Failed Deployment Details

#### `mater-maria` — Branch: `feat/unify-nexus-creative-campaigns`

Both preview deployments (`dpl_2HqZydLpVpd6FeFDLD3vMJvZRGJF`, `dpl_2KA8QkZUTF6TwVVzGeidUqguCdFg`) failed at `npm install` with the same error.

**Error excerpt (10:20:26 UTC):**
```
npm error code ERESOLVE
npm error ERESOLVE could not resolve

npm error While resolving: @eslint/js@10.0.1
npm error Found: eslint@9.39.4
npm error Could not resolve dependency:
npm error peerOptional eslint@"^10.0.0" from @eslint/js@10.0.1

npm error Conflicting peer dependency: eslint@10.6.0
npm error Fix the upstream dependency conflict, or retry this command with
npm error --force or --legacy-peer-deps to accept an incorrect resolution.

Error: Command "npm install" exited with 1
```

**Root cause:** `package.json` pins `eslint@^9.39.4` (v9) but also declares `@eslint/js@^10.0.1` as a dev dependency, which requires `eslint@^10.0.0` (v10). These two constraints are incompatible and npm strict resolution fails.

---

#### `platform` — Prior ERROR (now recovered)

The `feat(platform): unify nexus-creative-editorial campaign pages under /campaigns/` commit (`805d24142`) triggered a deployment that built successfully but was rejected by Vercel for exceeding the Hobby plan's **12 Serverless Function limit**. The follow-up commit `de65c6f` ("consolidate API routes to stay within Vercel Hobby 12-function limit") resolved it — `platform` is currently READY.

---

### Recommended Fix

**For `mater-maria` (blocking — no successful deploy on this branch):**

In `package.json`, align ESLint to a single major version. Choose one of:

**Option A — Stay on ESLint v9 (recommended, less churn):**
```json
"devDependencies": {
  "eslint": "^9.39.4",
  "@eslint/js": "^9.x.x"   // downgrade to match eslint v9
}
```

**Option B — Upgrade to ESLint v10:**
```json
"devDependencies": {
  "eslint": "^10.0.0",
  "@eslint/js": "^10.0.1"  // already correct
}
```

After updating `package.json`, delete `package-lock.json` and run `npm install` locally to regenerate a clean lockfile before pushing.
