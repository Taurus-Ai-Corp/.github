## Vercel Health Alert - 2026-08-28

| Project | Status | Last Deploy | Issues |
|---------|--------|-------------|--------|
| mater-maria | ❌ ERROR (branch) / ✅ READY (prod) | 2026-08-25 | 6 consecutive build failures on `feat/nexus-core-organic-design-system` — npm ERESOLVE |
| nexus-platform | ✅ READY | 2026-08-25 | None |
| taurus-payment-substrate | ✅ READY | ~2026-08-17 | None |
| All other 47 projects | ✅ No recent activity | — | None |

**Failed Deployment Details:**

**Project:** `mater-maria`
**Branch:** `feat/nexus-core-organic-design-system`
**Deployments failing:** 6 consecutive (IDs: `dpl_7HQEjLE4`, `dpl_7MTj2GRB`, `dpl_21iciYAS`, `dpl_FEgRfJ3m`, `dpl_2BudQ3ev`, `dpl_977Y5gAt`)
**First failure:** 2026-08-25 · **Latest failure:** 2026-08-25T11:14:41 UTC
**Production impact:** ⚠️ None yet — production still running last READY deploy (`dpl_HsyiZRHeZkvd8VAv8UGMw16Dhh2Z`). Any merge to production will fail.

**Build error (last 50 lines excerpt):**
```
npm error code ERESOLVE
npm error ERESOLVE could not resolve
npm error
npm error While resolving: @eslint/js@10.0.1
npm error Found: eslint@9.39.4
npm error node_modules/eslint
npm error   dev eslint@"^9.39.4" from the root project
...
npm error Could not resolve dependency:
npm error peerOptional eslint@"^10.0.0" from @eslint/js@10.0.1
npm error
npm error Conflicting peer dependency: eslint@10.9.1
npm error
npm error Fix the upstream dependency conflict, or retry this command
npm error with --force or --legacy-peer-deps
npm error
Error: Command "npm install" exited with 1
```

**Root Cause:**
`package.json` declares `@eslint/js@^10.0.1` (requires eslint ≥10) alongside `eslint@^9.39.4`. These versions are incompatible — ESLint 9 and `@eslint/js` v10 cannot coexist without `--legacy-peer-deps`.

**Recommended Fix:**

Option A (preferred — upgrade ESLint): Bump `eslint` to `^10.0.0` in `package.json`, verify all ESLint plugins are compatible with ESLint 10 (`@typescript-eslint/eslint-plugin@8.61.1` supports it), then push to the branch.

```diff
- "eslint": "^9.39.4",
+ "eslint": "^10.0.0",
```

Option B (quick unblock): Pin `@eslint/js` back to `^9.x` to match the installed ESLint version:
```diff
- "@eslint/js": "^10.0.1",
+ "@eslint/js": "^9.0.0",
```

Option C (CI workaround only): Add `--legacy-peer-deps` to Vercel's install command in `vercel.json` or project settings — avoids fixing the root conflict but unblocks deploys immediately.
