# Vercel Deployment Health Alert — 2026-09-05

**Generated:** 2026-09-05 (automated health monitor)
**Severity:** HIGH — 3 production projects stuck on ERROR deployment

---

## Summary

Three projects have **ERROR** as their most recent deployment, all caused by the same root issue: dependabot PR [#63](https://github.com/Taurus-Ai-Corp/GRIDERA/pull/63) on the GRIDERA repo bumped `@noble/post-quantum` as part of a 21-package minor/patch update. The new version changed its subpath exports, breaking the `@taurus/pqc-crypto` workspace package which imports from the now-missing `@noble/post-quantum/ml-dsa` and `@noble/post-quantum/ml-kem` paths. Every app that depends on `@taurus/pqc-crypto` fails at build time with TypeScript error TS2307.

**No deployments occurred in the last 24 hours** — the ERROR state predates this window (deployments were triggered ~Aug 31 2026). Production traffic is being served by the last successful READY deployments on `main`.

---

## Failed Deployments

| Project | Deployment ID | Trigger | Status |
|---|---|---|---|
| comply | `dpl_F4VauHHSu5JVJj5PsASNUecvKunB` | dependabot PR #63 | ERROR |
| landing | `dpl_Evx4ua3MYHbFjFDCiKBVH7meXcxu` | dependabot PR #63 | ERROR |
| q-grid-comply-ca | `dpl_2TGHTUeE6gBka8w3QRHWfUdMNqJp` | dependabot PR #63 | ERROR |

---

## Root Cause

**Package:** `packages/pqc-crypto` (`@taurus/pqc-crypto`)
**Failing dependency:** `@noble/post-quantum` (bumped by dependabot PR #63)

The package uses subpath imports that no longer exist after the version bump:

```
src/agility.ts(13,26): error TS2307: Cannot find module '@noble/post-quantum/ml-dsa'
src/kem.ts(1,27):      error TS2307: Cannot find module '@noble/post-quantum/ml-kem'
src/sign.ts(1,26):     error TS2307: Cannot find module '@noble/post-quantum/ml-dsa'
```

**Exit:** `@taurus/pqc-crypto#build` exits code 2 → turbo cascade fails all dependent apps.

---

## Current Production State

Production traffic is unaffected — each project falls back to its last successful READY deploy on `main`:

| Project | Fallback Deployment | Branch |
|---|---|---|
| comply | `dpl_EvTY29QKGK2aiYikreYHw6bUZgwh` | main |
| landing | `dpl_J9ezp16oHTyKxgBzazX8qbw71Knd` | main |
| q-grid-comply-ca | `dpl_6EYbFGanpaYGde6Ks23tcrYcgXfN` | main |

---

## Required Action

1. **Close or revert dependabot PR #63** on GRIDERA to unblock future merges to `main`, OR
2. **Fix `packages/pqc-crypto`** to use the updated `@noble/post-quantum` API:
   - Check the new package's export map (the subpath `/ml-dsa` and `/ml-kem` may have been renamed or merged)
   - Update imports in `src/agility.ts`, `src/kem.ts`, and `src/sign.ts`
   - Push fix to a branch, verify build passes on Vercel preview, then merge

**Priority:** The current `main` branch builds fine and serves production. This only blocks future `main` merges if PR #63 is not closed. If PR #63 is being held open, fix `pqc-crypto` imports first.

---

## All Projects Status (50 total)

47 projects healthy (most recent deployment: READY). 3 projects on ERROR (see above). 1 project (mater-maria) has ERRORs on a feature branch `feat/nexus-core-organic-design-system` but production deployment is READY.
