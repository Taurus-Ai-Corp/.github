# Vercel Deployment Health Report — 2026-07-04

**Generated:** 2026-07-04 (automated health check)
**Team:** Taurus's projects (`team_ljtVg59YsYUDbIdetyyOVg05`)
**Projects scanned:** 50
**Healthy:** 45 ✅ | **Failing:** 5 🚨

---

## Summary

| Project | Status | Deployment ID | Last Commit | Consecutive Failures |
|---------|--------|---------------|-------------|----------------------|
| **gridera** | 🔴 ERROR | `dpl_7z6KyBiC1yzZKVCCD3dzxbHtwCNo` | chore: remove internal file from public repo | **10** |
| **web** | 🔴 ERROR | `dpl_8hFsTWbdwedxUuDsG6vZzg9U9dMK` | fix: add API domain to CSP connect-src | **4** |
| **social-media-orchestra** | 🟠 BLOCKED | `dpl_9SGQpWwrNyPhqnaMKFxPGhuQGBtb` | feat: initial commit | **5** (never deployed) |
| **sma-api-prod** | 🟠 BLOCKED | `dpl_67syuYscT1srcuSoVy2DpiNcQ2NE` | feat: initial commit | **1** |
| **orca-api-backend** | 🟠 BLOCKED | `dpl_7CLy3MMACBhoLxgSvFGH1LquXcPT` | — | **1** |

---

## Critical Failures (ERROR)

### 1. `gridera` — Prisma WASM module missing

**Project ID:** `prj_41XVV5RXjQqa6mj6nzfVHWTq1mWj`
**Repo:** `gridera-comply` (branch: `main`)
**Commit SHA:** `23a0ecc1c6a35c588f09c3737c89a78c4b962fc5`
**Last successful production deploy:** June 2026

**Build error (tail log):**
```
02:24:51  npm error path /vercel/path0/products/gridera/web
02:24:51  npm error command failed
02:24:51  npm error command sh -c prisma generate
02:24:51  npm error Error:
02:24:51  npm error Cannot find module '/vercel/path0/node_modules/@prisma/client/runtime/query_engine_bg.postgresql.wasm-base64.js'
02:24:51  npm error Require stack:
02:24:51  npm error - /vercel/path0/node_modules/prisma/build/index.js
02:24:51  Error: Command "npm install" exited with 1
```

**Root cause:** `prisma generate` fails because `@prisma/client`'s WASM runtime file is missing from the hoisted `node_modules` at the monorepo root. The commit "chore: remove internal file from public repo" likely added a `.npmignore` or `.gitignore` rule that is excluding the Prisma-generated WASM file, or altered the monorepo workspace config so `@prisma/client` no longer installs correctly at the root level.

**Recommended fix:**
1. Check what was removed in commit `23a0ecc` — look for changes to `.npmignore`, `.gitignore`, `package.json` workspaces, or `prisma/schema.prisma`.
2. Ensure `@prisma/client` is listed as a dependency in `products/gridera/web/package.json` (not just devDependencies).
3. Add `outputDir` in `prisma/schema.prisma` to generate the client inside the sub-package rather than relying on root hoisting:
   ```prisma
   generator client {
     provider = "prisma-client-js"
     output   = "../node_modules/.prisma/client"
   }
   ```
4. Or pin `@prisma/client` and `prisma` to the same version in both root and sub-package `package.json`.

---

### 2. `web` — Next.js not detected (wrong Root Directory)

**Project ID:** `prj_2neVLDadIRMMnrhtyR6CZ1QvbQfH`
**Commit SHA:** `1f3e7d418bd434f2ce6501a24573dff8bb034884`
**Last successful production deploy:** ~May 2026

**Build error (tail log):**
```
13:29:34  Error: No Next.js version detected. Make sure your package.json has "next"
          in either "dependencies" or "devDependencies". Also check your Root Directory
          setting matches the directory of your package.json file.
```

**Root cause:** Vercel cannot find `next` in the `package.json` at the configured Root Directory. The commit "fix: add API domain to CSP connect-src" may have moved files or changed the project structure so that the Vercel Root Directory setting (`/`) no longer points to the directory containing the Next.js `package.json`.

**Recommended fix:**
1. Go to Vercel → Project `web` → Settings → General → Root Directory.
2. Update it to the subdirectory that contains `package.json` with `"next"` as a dependency (e.g. `apps/web`, `frontend`, etc.).
3. Alternatively, if the project was restructured: verify `next` is present in the correct `package.json` at the root.

---

## Blocked Deployments (BLOCKED — never reached build phase)

BLOCKED status means Vercel rejected the deployment before build started. No build logs were generated. Common causes: concurrent build limits exceeded, project suspended, or spend limits reached.

### 3. `social-media-orchestra`

**Project ID:** `prj_XPueWKRUHgeVqD1r4RCAUe04caSP`
**Status:** BLOCKED across all 5 deployments — this project has **never successfully deployed**.
**Recommended fix:** Check Vercel project settings for suspension or misconfigured build settings. Verify the repository `social-media-orchestra` is properly linked and the framework is correctly detected.

### 4. `sma-api-prod`

**Project ID:** `prj_00oCC5FJji5jNC8SuUxF10IVUA3Z`
**Status:** Latest deployment BLOCKED (production); previous deployment is READY.
**Recommended fix:** Re-trigger deployment. If BLOCKED again, check team spend limits or concurrent build quota in Vercel dashboard.

### 5. `orca-api-backend`

**Project ID:** `prj_libiGnavdUq2Y9956NEno0LceCVn`
**Status:** Latest BLOCKED (preview), previous ERROR (production) — no valid production deployment.
**Recommended fix:** Check Vercel project settings and re-trigger. Review what caused the prior ERROR state on the production deployment as well.

---

## Healthy Projects (45/50)

All other projects returned `READY` as their latest production deployment state. No action required.

---

## Action Items

| Priority | Owner | Action |
|----------|-------|--------|
| 🔴 P0 | Engineering | Fix `gridera` Prisma WASM issue — production broken for 10 deployments |
| 🔴 P0 | Engineering | Fix `web` Root Directory setting — production broken for 4 deployments |
| 🟠 P1 | DevOps | Investigate `social-media-orchestra` — never successfully deployed |
| 🟠 P1 | DevOps | Re-trigger `sma-api-prod` and `orca-api-backend`, check spend/quota limits |
