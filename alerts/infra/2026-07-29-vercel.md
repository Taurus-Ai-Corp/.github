## Vercel Health Alert - 2026-07-29

| Project | Status | Last Deploy | Issues |
|---------|--------|-------------|--------|
| landing | ⚠️ ERRORS (preview) | 2026-07-29 | 6 dependabot PR builds failing (day 2) |
| comply | ⚠️ ERRORS (preview) | 2026-07-29 | 6 dependabot PR builds failing (day 2) |
| q-grid-comply-ca | ⚠️ ERRORS (preview) | 2026-07-29 | 6 dependabot PR builds failing (day 2) |
| frontend | ✅ READY | 2026-07-29 | None |
| 46 other projects | ✅ No activity | No deployments in 24h | None |

> **Production is healthy.** A new production build from `main` (commit `00902ed5`) deployed successfully to all 3 GRIDERA apps today. All failing deployments are from unmerged dependabot preview branches.

---

## Failed Deployment Details

All three affected projects (`landing`, `comply`, `q-grid-comply-ca`) share the **GRIDERA** monorepo. The same dependabot PRs that failed yesterday are re-running and failing again today (new pushes triggered re-runs).

### Error 1 — `minor-and-patch` group bump (PR #48) — latest failure

**Deployment:** `dpl_CxtrFnWgeJ4JKJzLSWk2orHgUycE` (landing, most recent)  
**Branch:** `dependabot/npm_and_yarn/minor-and-patch-2028fde2b0`  
**Packages bumped:** 18 packages including `@taurus-ai/swarm-spawner` 0.4.0→0.5.1, `drizzle-orm` 0.39.3→0.45.2, `next` 16.2.9→16.2.12, `react` 19.2.7→19.2.8

```
@taurus/pqc-crypto:build: src/agility.ts(13,26): error TS2307:
  Cannot find module '@noble/post-quantum/ml-dsa' or its corresponding type declarations.
@taurus/pqc-crypto:build: src/kem.ts(1,27): error TS2307:
  Cannot find module '@noble/post-quantum/ml-kem' or its corresponding type declarations.
@taurus/pqc-crypto:build: src/sign.ts(1,26): error TS2307:
  Cannot find module '@noble/post-quantum/ml-dsa' or its corresponding type declarations.
@taurus/pqc-crypto:build: ELIFECYCLE Command failed with exit code 2.
ERROR: Command "cd ../.. && pnpm turbo run build --filter=landing" exited with 2
```

Root cause: `@taurus-ai/swarm-spawner` 0.5.x likely brought an incompatible version of `@noble/post-quantum`, causing subpath exports `ml-dsa`/`ml-kem` to resolve incorrectly inside `packages/pqc-crypto`.

### Error 2 — `typescript` 5.9.3 → 7.0.2 (PR #52)

**Deployment:** `dpl_AYi6RWTVcm2sLeTyE3EZNPST3FCv` (landing)  
**Branch:** `dependabot/npm_and_yarn/typescript-7.0.2`

```
landing:build: Failed to compile.
landing:build: Module not found: Can't resolve '@/components/nav'
landing:build: Module not found: Can't resolve '@/components/product-shell'
landing:build: Module not found: Can't resolve '@/components/product-hero'
landing:build: Module not found: Can't resolve '@/components/product-section'
landing:build: ELIFECYCLE Command failed with exit code 1.
ERROR: Command "cd ../.. && pnpm turbo run build --filter=landing" exited with 1
```

Root cause: TypeScript 7 introduces breaking changes to path alias / module resolution that break Next.js `@/` path aliases.

### Other failing dependabot PRs (same pattern as yesterday)
- PR #55 — `stripe` 21.0.1 → 22.3.2 (Stripe API version type mismatch)
- PR #54 — `@noble/hashes` 1.8.0 → 2.2.0 (breaks PQC crypto imports)
- PR #53 — `@neondatabase/serverless` 0.10.4 → 1.1.0 (major API change)
- PR #48 — 18-package minor-and-patch group (fails due to swarm-spawner/pqc-crypto conflict)

---

## Recommended Fix

**Production is not affected — production `main` is READY.** These PRs have now been failing for 2 consecutive days, indicating they need manual attention to avoid becoming stale.

1. **PR #48 (minor-and-patch group) / PR #54 (@noble/hashes 2.x):**  
   In `packages/pqc-crypto`, check what version of `@noble/post-quantum` is installed and whether it's compatible with `@noble/hashes` 2.x. Either add an explicit `@noble/post-quantum` resolution in the root `package.json`, or pin `@noble/hashes` back to `^1.x` in the workspace.

2. **PR #52 (TypeScript 7.0.2):**  
   TypeScript 7 changed module resolution behaviour. Add explicit `moduleResolution: "bundler"` (or `"node16"`) in `tsconfig.json` and verify `@/` path alias configuration. This is likely a one-line tsconfig fix.

3. **PR #55 (stripe 22.3.2):**  
   Update the Stripe API version string passed to the Stripe constructor to match the version expected by stripe-node v22.

4. **PR #53 (@neondatabase/serverless 1.x):**  
   Review breaking changes in the Neon serverless changelog and update call sites.

**Action:** These PRs should not be auto-merged. Assign for manual review — or close them and re-open once the upstream compatibility issues are resolved. If left unaddressed, dependabot will keep re-running them and accumulating failed preview deployments.
